import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sasquatchlabs.selectasquatch',
  appName: 'Select A Squatch',
  webDir: 'dist'
};

export default config;
